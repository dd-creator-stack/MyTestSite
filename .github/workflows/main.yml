name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:

  # -----------------------
  # BUILD + TEST
  # -----------------------
  build:
    runs-on: ubuntu-latest

    steps:
